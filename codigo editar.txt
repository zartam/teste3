
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Bem</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; padding: 0; }
        form { max-width: 400px; margin: auto; }
        label, input, select { display: block; width: 100%; margin-top: 10px; }
        button { background-color: #007bff; color: white; padding: 10px; border: none; margin-top: 15px; cursor: pointer; }
    </style>
</head>
<body>
    <h1>Editar Bem</h1>
    <form action="{{ url_for('editar', id=bem.id) }}" method="post" enctype="multipart/form-data">
        <label>Nome do Bem:</label>
        <input type="text" name="nome_bem" value="{{ bem.nome }}" required>

        <label>Data da Compra:</label>
        <input type="date" name="data_compra" value="{{ bem.data_compra }}" required>

        <label>Categoria:</label>
        <input type="text" name="categoria" value="{{ bem.categoria }}" required>

        <label>Status:</label>
        <select name="status">
            <option value="Disponível" {% if bem.status == 'Disponível' %}selected{% endif %}>Disponível</option>
            <option value="Vendido" {% if bem.status == 'Vendido' %}selected{% endif %}>Vendido</option>
            <option value="Quebrado" {% if bem.status == 'Quebrado' %}selected{% endif %}>Quebrado</option>
        </select>

        <label>Foto:</label>
        <input type="file" name="foto">

        <label>Valor de Compra (R$):</label>
        <input type="number" step="0.01" name="valor_compra" value="{{ bem.valor_compra }}" required>

        <label>Vida Útil (anos):</label>
        <input type="number" name="vida_util" value="{{ bem.vida_util }}" required>

        <button type="submit">Salvar Alterações</button>
    </form>
<a href="{{ url_for('index') }}" style="display: block; text-align: center; margin-top: 20px;">Voltar para Home</a>
</body>
</html>